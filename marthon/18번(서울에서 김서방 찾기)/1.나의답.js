function solution(seoul) {
    let check = seoul.indexOf("Kim");
        //indexOf메소드를 이용하여 Kim 배열에서 index값을구한다.
    let say= `김서방은 ${check}에 있다`
        //템플릿 리터럴을 적용하여 문자열과 변수를 쉽게 변수에적용
return say 
}